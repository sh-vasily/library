<UserControl x:Class="DbDemo.Controls.BooksControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:DbDemo.ViewModels"
             FontSize="40"
             d:DataContext="{d:DesignInstance viewModels:BooksControlViewModel}">
    <UserControl.DataContext>
        <viewModels:BooksControlViewModel />
    </UserControl.DataContext>
   <DataGrid ItemsSource="{Binding Books}" AutoGenerateColumns="False">
           <DataGrid.Columns>
               <DataGridTextColumn Header="Id" Width="100" Binding="{Binding Id}"/>
               <DataGridTextColumn Header="Title" Width="400" Binding="{Binding Title}"/>
               <DataGridTextColumn Header="Author" Width="400" Binding="{Binding Author}"/>
               <DataGridTextColumn Header="ISBN" Width="200" Binding="{Binding Isbn}"/>
           </DataGrid.Columns>
       </DataGrid>
</UserControl>